/**
 * Builds the catalog the CLI fetches: scans /reflexes for reflex.json manifests
 * and emits a single registry.json. (Implementation lands with the `add` command.)
 */
export const REGISTRY_VERSION = 1;

# stay-in-repo 📁

**Edits stay inside the project.**

Blocks an agent from writing or editing files outside the project root — absolute paths
like `/etc/hosts`, or escapes like `../other/app.ts`. In-repo paths pass through.

```bash
npx agentreflex add stay-in-repo
```

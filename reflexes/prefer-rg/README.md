# prefer-rg 🔍

**Reach for ripgrep.**

Blocks recursive `grep -r` / `grep -R` and points you at `rg` instead — faster, and it
respects `.gitignore`. Non-recursive `grep` on a file is left alone.

```bash
npx agentreflex add prefer-rg
```

{
  "name": "@agentreflex/ask-on-prod",
  "version": "0.0.0",
  "description": "Pauses before commands that deploy or publish to production.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "exports": { ".": { "import": "./dist/index.js" } },
  "private": true,
  "files": ["dist", "reflex.json"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentreflex/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
